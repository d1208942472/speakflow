{
  "name": "speakflow",
  "private": true,
  "workspaces": ["apps/web", "packages/*"],
  "scripts": {
    "web": "cd apps/web && npm run dev",
    "api": "cd apps/api && .venv/bin/uvicorn main:app --reload --port 8000"
  }
}
